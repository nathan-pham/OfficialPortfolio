const Section = ({ children }) => {
    return (
        <section>
            <div className="root-block">
                { children }
            </div>
        </section>
    )
}

export default Section